# Empty means same-origin requests. Vite proxies them to the local FastAPI server.
VITE_API_BASE_URL=
VITE_AUTH_MODE=demo
# Public SPA config only; use a real Auth0 API audience, not the SPA client ID.
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
