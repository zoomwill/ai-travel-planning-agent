# Prompt P06 — Router and Requirement Collection

Complete only Phase 06.

Use deterministic rule routing by default with optional LLM interface. Support incomplete plan, full plan, simple lookup, modify plan and unrelated. Ask one missing-field question at a time, preserve fields, route completed requirements to planning, avoid full planner for simple lookup, add table-driven and multi-turn tests, document Router versus Handoff. Run all checks and stop.
