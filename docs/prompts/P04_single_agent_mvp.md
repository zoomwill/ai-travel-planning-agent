# Prompt P04 — Single-Agent Planning MVP

Complete only Phase 04.

Add `POST /api/v1/plans/mock`, accept a complete request, call deterministic mock providers, generate a structured multi-day itinerary with ordinary Python rules, return JSON plus Markdown, add API/unit/end-to-end deterministic tests and explain data flow. Do not add LangGraph, MCP, RAG, SSE or real APIs. Run all checks and stop.
