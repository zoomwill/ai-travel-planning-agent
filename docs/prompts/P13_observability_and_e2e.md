# Prompt P13 — Observability and End-to-End Verification

Complete only Phase 13.

Add structured Loguru logs with request/thread IDs and secret redaction; Prometheus metrics for HTTP, graph, LLM, token, RAG cache, MCP and reviews; Grafana provisioning; optional environment-controlled LangSmith; a deterministic end-to-end test covering incomplete request, follow-up, parallel search, RAG, failed review, revision, streaming and persistence; one-command startup; final architecture and limitations. Run every test, show actual results and stop.
