# Prompt P11 — PostgreSQL Persistence and Long-Term Memory

Complete only Phase 11 using current LangGraph persistence integrations.

Add PostgreSQL checkpointer, persistent user memory, travel plan and profile persistence, typed classification into stable long-term/trip-specific/not-memory, relevant-memory retrieval and injection, contradiction policy, deletion/correction and tests for cross-thread persistence and contamination prevention. Keep Redis as cache, not authoritative memory. Run all checks and stop.
