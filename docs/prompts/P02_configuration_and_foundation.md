# Prompt P02 — Configuration and Application Foundation

Complete only Phase 02. Use current official APIs.

Add async PostgreSQL, Redis and Chroma clients; typed settings; safe secrets; FastAPI startup/shutdown lifecycle; `/health/live`; `/health/ready`; structured exceptions; unit tests with mocks; one opt-in Docker integration test; secret redaction tests; implementation notes. Run all checks and stop.
