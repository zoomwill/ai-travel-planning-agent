# Prompt P14 — Optional Frontend and Real Providers

This phase is optional. First ask which one I want: simple web UI, real Qwen, real weather/maps, licensed flight/hotel provider, authentication or deployment. Do not implement all automatically. For the chosen item, propose the smallest secure design, preserve mocks, add provider interfaces, cost/rate safeguards, opt-in integration tests, docs and checks. Stop.
