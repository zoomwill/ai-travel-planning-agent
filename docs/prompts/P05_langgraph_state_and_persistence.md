# Prompt P05 — LangGraph State and Persistence

Complete only Phase 05. Inspect current official LangGraph docs and record source/API differences.

Add LangGraph, define typed TravelPlanState, move deterministic steps into nodes, add START/END, compile with in-memory checkpointer, use current thread ID config, expose graph invocation and state-history services, test checkpointing/resume/isolation, add Mermaid graph. Do not add Router, RAG, MCP, parallel nodes or real LLM calls. Run all checks and stop.
