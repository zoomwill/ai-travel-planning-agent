# Prompt P01 — Infrastructure Baseline

Complete only Phase 01. Explain every command before running it.

Inspect Docker Compose and environment files, start PostgreSQL, Redis and Chroma, verify each independently, confirm volumes, add a beginner infrastructure check script, document start/stop/reset and port conflicts, run all checks and stop with a commit message. Do not add application clients or LangGraph.
