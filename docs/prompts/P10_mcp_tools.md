# Prompt P10 — MCP Tool Services

Complete only Phase 10 after consulting current FastMCP docs.

Create a local stdio MCP server for weather/map and a local HTTP server for flights/hotels/attractions. Expose typed mock tools, discover both through one client, handle namespaces/conflicts, timeouts/errors/lifecycle, test business logic and local integrations, convert tools for the Agent layer and document Function Calling versus MCP. No real travel APIs. Run all checks and stop.
